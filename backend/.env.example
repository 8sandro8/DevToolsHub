PORT=4001
NODE_ENV=development
JWT_SECRET=your_super_secret_key_here_change_in_production
JWT_EXPIRES_IN=24h
DB_PATH=./data/devtools.db
UPLOADS_DIR=./uploads
CORS_ORIGIN=*

# GitHub API Token (opcional — aumenta el rate limit de 60 a 5000 req/hora)
GITHUB_TOKEN=
